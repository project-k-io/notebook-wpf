<UserControl x:Class="Projects.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ToolBar Grid.Row="0">
            <CheckBox Content="Optimize" IsChecked="{Binding UseTimeOptimization}"
                      Command="{Binding CommandGenerateReport}" />
        </ToolBar>
        <TextBox TextWrapping="Wrap" AcceptsReturn="true" FontFamily="Courier New" Grid.Row="1"
                 Text="{Binding Report}" />
    </Grid>
</UserControl>